## 0.2.0 [Breaking Change]
- Gooey now uses a GLSL shader instead of color filters + blur, so most previous parameters are no longer relevant.
## 0.1.1
- Added `overpaintFactor` parameter for extra paint margin around blobs
- Added `sharpness` parameter for controlling edge sharpness
- Normalized threshold to have more effect on alpha cut-off.  

## 0.1.0
- Initial release
